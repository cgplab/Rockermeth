#' Run all ROCker-meth analysis
#'
#' A simple wrapper to perform a complete analysis with rocker meth. It doesn't
#' return anything but saves two files for further analysis and evaluation.
# auc <- compute_AUC(tumor_table, control_table, nclust, na_frac)
# find DMR
# all_dmr <- whole_genome_segmentator(tumor_table, control_table, auc, reference_table, na_cutoff, length_cutoff)
# compute sample-score
# z_scores <- compute_z_score(tumor_table, control_table, all_dmr, reference_table, out_folder = NULL)

